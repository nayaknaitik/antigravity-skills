package {{PACKAGE_NAME}}.infrastructure.adapter.out.persistence;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.Optional;
import java.util.UUID;

@Repository
public interface SpringData{{RESOURCE_NAME}}Repository extends JpaRepository<{{RESOURCE_NAME}}Entity, UUID> {

    @Query("SELECT e FROM {{RESOURCE_NAME}}Entity e WHERE e.id = :id")
    Optional<{{RESOURCE_NAME}}Entity> findByIdWithDetails(@Param("id") UUID id);
}
